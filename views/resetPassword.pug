doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario 
            h1= nombrePagina
            p Ingresa tu Nueva Contraseña

            form.caja-login(method="POST")
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder=" Tu Nueva Contraseña" )
                .campo.enviar
                    input.boton(type="submit" value="Restablecer Contraseña")